exports.createPages = async ({ actions }) => {
  // Dynamic pages are handled by Gatsby file-based routing
  // Pages are created automatically from src/pages/story/{storyId}.js
  // and src/pages/quiz/{storyId}.js
}
